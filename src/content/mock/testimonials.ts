import type { Testimonial } from "../schema";

/**
 * Production testimonials — empty.
 * Prefer Member Story / Transformation models (ADR-022).
 */
export const mockTestimonials: Testimonial[] = [];

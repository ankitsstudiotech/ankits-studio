import type { PricingPlan } from "../schema";

/**
 * Programme-specific monthly/quarterly/annual fees remain pending.
 * Illustrative rupee plan rows were removed so they cannot be mistaken for
 * real membership prices. Registration fee lives on StudioCommercial (INR 300).
 */
export const mockPricingPlans: PricingPlan[] = [];

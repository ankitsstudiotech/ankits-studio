import type { Transformation } from "../schema";

/**
 * Production transformations — empty until consented, evidence-gated records exist.
 * Illustrative journeys live only in `src/content/fixtures/illustrative-evidence.ts`.
 */
export const mockTransformations: Transformation[] = [];
